import numpy as np
import matplotlib.pyplot as plt

import sys #gives access to c like sys library :D
import os #gives access to operating system ! :D

print(sys.argv) #prints any command line arguments! :D
print(os.getcwd()) # prints the current working directory :}